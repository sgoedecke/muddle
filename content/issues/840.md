---
number: 840
title: "Flaky test: IngestSpec fails ~3% of CI runs"
labels: [flaky-test, ci]
state: open
author: the-other-sam
opened: Nov 2, 2024
---

`IngestSpec > backpressure > drops oldest when full` fails intermittently, roughly 3% of runs. It's almost certainly a timing assumption in the test rather than a product bug, but it's eroding trust in CI — people have started re-running until green, which is how real failures slip through.

I *think* the flakiness is exposing the real race in #812 / #948 rather than being purely a test issue, so I don't want to just add a `sleep` and move on.

@@ maria-alvarez | Nov 3, 2024
Strong agree on not papering over it. If it's surfacing #812 that's valuable signal. Can you capture a failing seed?

@@ the-other-sam | Nov 4, 2024
Got one: `SEED=0x4f2a`. Reproduces about 1 in 12 with that seed. Attaching to #812 as well since that's where the write-side analysis lives.

@@ sam-oduya | Nov 7, 2024
This also fails more often right after a deploy, which lines up with the cold-cache behaviour in #877. Might be three issues wearing a trenchcoat.
