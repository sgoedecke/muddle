---
number: 840
title: "Flaky test fails ~3% of CI runs"
labels: [flaky-test, ci, testing]
state: open
author: the-other-sam
opened: Nov 2, 2024
---

`IngestSpec > drops oldest when full` fails intermittently, ~3% of runs. It's eroding trust in CI — people re-run until green, which is how real failures slip through.

I think it's surfacing the real race in #812 rather than being purely a test bug, so I don't want to just add a `sleep`. It also fails more right after a deploy, which lines up with the cold-cache behaviour in #877. And on a fresh checkout it can't go green at all until the reproducible-build issue in #948 is fixed — bad first impression for the onboarding problem in #918.

@@ maria-alvarez | Nov 3, 2024
Strong agree on not papering over it. If it's exposing #812 that's valuable signal. Capture a failing seed?

@@ the-other-sam | Nov 4, 2024
Got one: `SEED=0x4f2a`, ~1 in 12. Attaching to #812. Might be three issues in a trenchcoat.
