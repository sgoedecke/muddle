---
number: 918
title: "Local dev setup takes new hires ~2 days"
labels: [docs, onboarding, dx]
state: open
author: priya-nair
opened: Oct 15, 2024
---

Our last three new hires each spent ~2 full days getting a working local environment. The README says "run `make dev`" and then eleven undocumented steps happen in someone's head. We're filtering out contributors before they write a line of code.

Concretely, setup is blocked by: wrong date examples until #856 lands, fixtures that crash the parser per #819, CLI calls that silently fail per #911, and a build that isn't reproducible per #948 (so a fresh checkout can't go green — see the flaky #840).

@@ the-other-sam | Oct 16, 2024
+1. Nobody trusts a red test suite on a fresh clone, so they assume they broke something. Terrible first impression.

@@ dmitri-k | Oct 18, 2024
Proposal: a reproducible dev container plus a "good first issue" list. Half of what we've been discussing (#856, #911, #925) would make reasonable first issues once scoped.
