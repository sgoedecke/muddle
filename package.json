{
  "name": "tarpit",
  "version": "0.1.0",
  "description": "A generic LLM agent tarpit. Static site, deployable to GitHub Pages.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "serve": "node bench/server.js",
    "bench": "node bench/run.js"
  },
  "dependencies": {
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
